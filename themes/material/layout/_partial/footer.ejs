<% if(theme.footer){ %>
  <p><%= theme.footer %></p>
<% } %>

<p>
	积少成多
</p>
<p>
  &copy; <%= new Date().getFullYear() %> <a href="<%= config.url + config.root %>"><% if (config.author){ %> <%= config.author %> <% } else { %> <%= config.title %> <% } %></a>
</p>
<a id="gotop" href="#" title="back to top"><i class="mdi-hardware-keyboard-arrow-up"></i></a>
